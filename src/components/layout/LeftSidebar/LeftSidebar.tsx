import React, { memo, useCallback, useState } from 'react';\nimport { useChatStore, selectFilteredChats, selectCurrentChat } from '../../../stores/simpleChatStore';\nimport { Avatar } from '../../shared/Avatar';\nimport { Badge } from '../../shared/Badge';\nimport { Button } from '../../shared/Button';\nimport { Input } from '../../shared/Input';\nimport { BaseComponentProps, Chat } from '../../../types';\n\nexport interface LeftSidebarProps extends BaseComponentProps {\n  collapsed?: boolean;\n}\n\nexport const LeftSidebar = memo<LeftSidebarProps>(({ \n  collapsed = false,\n  className = '',\n  ...props \n}) => {\n  const { \n    onChatSelect, \n    onSearchChange, \n    onUserProfileToggle,\n    onSettingsToggle,\n    currentChatId,\n    searchQuery,\n    showUserProfile,\n    showChatSettings\n  } = useChatStore();\n  \n  const filteredChats = useChatStore(selectFilteredChats);\n  const currentChat = useChatStore(selectCurrentChat);\n  const [hoveredChatId, setHoveredChatId] = useState<string | null>(null);\n\n  const handleChatClick = useCallback((chatId: string) => {\n    onChatSelect(chatId);\n  }, [onChatSelect]);\n\n  const handleSearchChange = useCallback((event: React.ChangeEvent<HTMLInputElement>) => {\n    onSearchChange(event.target.value);\n  }, [onSearchChange]);\n\n  const formatLastActivity = (date: Date) => {\n    const now = new Date();\n    const diff = now.getTime() - date.getTime();\n    const days = Math.floor(diff / (1000 * 60 * 60 * 24));\n    const hours = Math.floor(diff / (1000 * 60 * 60));\n    const minutes = Math.floor(diff / (1000 * 60));\n\n    if (days > 0) return `${days}天前`;\n    if (hours > 0) return `${hours}小时前`;\n    if (minutes > 0) return `${minutes}分钟前`;\n    return '刚刚';\n  };\n\n  const renderChatItem = (chat: Chat) => {\n    const isActive = chat.id === currentChatId;\n    const isHovered = chat.id === hoveredChatId;\n    const hasUnread = chat.unreadCount > 0;\n\n    return (\n      <div\n        key={chat.id}\n        className={`\n          flex items-center p-3 cursor-pointer transition-all duration-200\n          hover:bg-gray-50 relative\n          ${isActive ? 'bg-blue-50 border-r-2 border-blue-500' : ''}\n          ${hasUnread ? 'bg-blue-50/30' : ''}\n        `}\n        onClick={() => handleChatClick(chat.id)}\n        onMouseEnter={() => setHoveredChatId(chat.id)}\n        onMouseLeave={() => setHoveredChatId(null)}\n      >\n        <div className=\"relative\">\n          <Avatar\n            src={chat.avatar}\n            alt={chat.name}\n            size={collapsed ? 'small' : 'medium'}\n            showStatus={chat.type === 'private'}\n            status={chat.isOnline ? 'online' : 'offline'}\n          />\n          \n          {hasUnread && (\n            <Badge\n              count={chat.unreadCount}\n              size=\"small\"\n              position=\"top-right\"\n              offset={[2, -2]}\n            />\n          )}\n        </div>\n\n        {!collapsed && (\n          <div className=\"flex-1 min-w-0 ml-3\">\n            <div className=\"flex items-center justify-between mb-1\">\n              <h3 className={`\n                text-sm font-medium truncate\n                ${hasUnread ? 'text-gray-900 font-semibold' : 'text-gray-700'}\n              `}>\n                {chat.name}\n              </h3>\n              <span className=\"text-xs text-gray-500 ml-2 flex-shrink-0\">\n                {formatLastActivity(chat.lastActivity)}\n              </span>\n            </div>\n            \n            {chat.lastMessage && (\n              <p className={`\n                text-sm truncate\n                ${hasUnread ? 'text-gray-800 font-medium' : 'text-gray-500'}\n              `}>\n                {chat.lastMessage.type === 'text' \n                  ? chat.lastMessage.content \n                  : `[${chat.lastMessage.type}]`\n                }\n              </p>\n            )}\n          </div>\n        )}\n\n        {isActive && (\n          <div className=\"w-2 h-8 bg-blue-500 rounded-l-full absolute right-0\" />\n        )}\n      </div>\n    );\n  };\n\n  const renderUserProfile = () => {\n    if (!showUserProfile) return null;\n\n    return (\n      <div className=\"border-t border-gray-200 p-4 bg-gray-50\">\n        <div className=\"flex items-center space-x-3\">\n          <Avatar\n            src=\"https://ui-avatars.com/api/?name=User&background=0066cc&color=fff\"\n            alt=\"Current User\"\n            size=\"large\"\n            showStatus\n            status=\"online\"\n          />\n          <div className=\"flex-1\">\n            <h4 className=\"font-medium text-gray-900\">当前用户</h4>\n            <p className=\"text-sm text-gray-500\">在线</p>\n            <p className=\"text-xs text-gray-400 mt-1\">user@example.com</p>\n          </div>\n        </div>\n        \n        <div className=\"mt-4 space-y-2\">\n          <Button\n            variant=\"ghost\"\n            size=\"small\"\n            fullWidth\n            onClick={() => console.log('Edit profile')}\n          >\n            编辑个人资料\n          </Button>\n          <Button\n            variant=\"ghost\"\n            size=\"small\"\n            fullWidth\n            onClick={() => console.log('Settings')}\n          >\n            设置\n          </Button>\n        </div>\n      </div>\n    );\n  };\n\n  const renderChatSettings = () => {\n    if (!showChatSettings || !currentChat) return null;\n\n    return (\n      <div className=\"border-t border-gray-200 p-4 bg-gray-50\">\n        <h4 className=\"font-medium text-gray-900 mb-3\">会话设置</h4>\n        <div className=\"space-y-2\">\n          <Button\n            variant=\"ghost\"\n            size=\"small\"\n            fullWidth\n            onClick={() => console.log('Mute chat')}\n          >\n            静音会话\n          </Button>\n          <Button\n            variant=\"ghost\"\n            size=\"small\"\n            fullWidth\n            onClick={() => console.log('Archive chat')}\n          >\n            归档会话\n          </Button>\n          <Button\n            variant=\"ghost\"\n            size=\"small\"\n            fullWidth\n            onClick={() => console.log('Delete chat')}\n            className=\"text-red-600 hover:text-red-700 hover:bg-red-50\"\n          >\n            删除会话\n          </Button>\n        </div>\n      </div>\n    );\n  };\n\n  return (\n    <aside \n      className={`\n        flex flex-col bg-white border-r border-gray-200 h-full\n        transition-all duration-300 ease-in-out\n        ${collapsed ? 'w-16' : 'w-80'}\n        ${className}\n      `}\n      {...props}\n    >\n      {/* Search Bar */}\n      {!collapsed && (\n        <div className=\"p-4 border-b border-gray-200\">\n          <Input\n            id=\"chat-search\"\n            type=\"search\"\n            placeholder=\"搜索会话或联系人...\"\n            value={searchQuery}\n            onChange={handleSearchChange}\n            leftIcon={\n              <svg className=\"w-4 h-4\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\">\n                <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} \n                      d=\"M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z\" />\n              </svg>\n            }\n            clearable\n            onClear={() => onSearchChange('')}\n          />\n        </div>\n      )}\n\n      {/* Chat List */}\n      <div className=\"flex-1 overflow-y-auto\">\n        {filteredChats.length === 0 ? (\n          <div className=\"flex flex-col items-center justify-center h-32 text-gray-500\">\n            {!collapsed && (\n              <>\n                <svg className=\"w-8 h-8 mb-2\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\">\n                  <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} \n                        d=\"M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z\" />\n                </svg>\n                <p className=\"text-sm\">暂无会话</p>\n              </>\n            )}\n          </div>\n        ) : (\n          <div className=\"divide-y divide-gray-100\">\n            {filteredChats.map(renderChatItem)}\n          </div>\n        )}\n      </div>\n\n      {/* Settings & Profile Toggle Buttons */}\n      {!collapsed && (\n        <div className=\"border-t border-gray-200 p-2 flex space-x-1\">\n          <Button\n            variant=\"ghost\"\n            size=\"small\"\n            onClick={onUserProfileToggle}\n            className={showUserProfile ? 'bg-blue-50 text-blue-600' : ''}\n            icon={\n              <svg className=\"w-4 h-4\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\">\n                <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} \n                      d=\"M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z\" />\n              </svg>\n            }\n          >\n            用户画像\n          </Button>\n          \n          <Button\n            variant=\"ghost\"\n            size=\"small\"\n            onClick={onSettingsToggle}\n            className={showChatSettings ? 'bg-blue-50 text-blue-600' : ''}\n            icon={\n              <svg className=\"w-4 h-4\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\">\n                <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} \n                      d=\"M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z\" />\n                <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} d=\"M15 12a3 3 0 11-6 0 3 3 0 016 0z\" />\n              </svg>\n            }\n          >\n            会话设置\n          </Button>\n        </div>\n      )}\n\n      {/* User Profile Panel */}\n      {renderUserProfile()}\n      \n      {/* Chat Settings Panel */}\n      {renderChatSettings()}\n    </aside>\n  );\n});\n\nLeftSidebar.displayName = 'LeftSidebar';